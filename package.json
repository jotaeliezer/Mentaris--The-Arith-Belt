{
  "name": "asteroid-blaster",
  "private": true,
  "version": "0.1.0",
  "description": "Math arcade space shooter.",
  "author": "ATMC",
  "main": "electron/main.js",
  "scripts": {
    "build": "node scripts/write_supabase_config.js && node scripts/build.js",
    "docs": "node scripts/write_supabase_config.js && node scripts/publish_docs.js",
    "watch": "node scripts/write_supabase_config.js && node scripts/build.js --watch",
    "dev:workspace": "node scripts/init_dev_workspace.js",
    "dev:serve": "npx --yes serve dev/public -l 4173",
    "electron": "electron .",
    "dist": "npm run build && electron-builder"
  },
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.atmc.asteroidblaster",
    "productName": "Asteroid Blaster",
    "directories": {
      "output": "dist"
    },
    "files": [
      "public/**/*",
      "electron/**/*",
      "package.json"
    ],
    "asar": true,
    "win": {
      "target": "nsis"
    }
  },
  "dependencies": {
    "esbuild": "^0.20.2",
    "phaser": "^3.90.0"
  }
}
